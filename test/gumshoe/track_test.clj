(ns gumshoe.track-test
  (:require [clojure.test :refer :all]
            [gumshoe.track :refer (deft)]))

;; Tests are in ../clojure, ported from the clojure/clojure repo

(deftest a-test
  (is (= 1 1)))
